<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LaoshiTest" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/grey</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="TextAppearance.App.Button" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="android:textSize">8sp</item>
        <item name="android:padding">5sp</item>
    </style>
    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cardCornerRadius">8dp</item>
        <item name="cornerSize">8dp</item>
        <item name="cardBackgroundColor">@color/white_transparent</item>
        <item name="cardElevation">0dp</item>
    </style>
    <style name="TypeButton" parent="Widget.MaterialComponents.Button.OutlinedButton.Icon">
        <item name="cornerFamily">rounded</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="cornerSize">8dp</item>
        <item name="strokeWidth">0dp</item>
        <item name="iconPadding">2dp</item>
        <item name="android:padding">2dp</item>
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>